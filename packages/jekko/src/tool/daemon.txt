Daemon runtime tools are only available inside ZYAL daemon incubator pass prompts. They write structured scratch memory and pass receipts for the host-owned daemon controller. They cannot stop a daemon or promote a task.

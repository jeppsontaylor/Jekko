# Jankurai Worktree Audit

Date: 2026-05-07
Assignee: Codex
Scope: worker 3 only

## Snapshot

- `rtk git status --porcelain=v1`
- `rtk git diff --stat`
- `rtk git diff --name-status`
- `rtk git diff --check`
- `rtk git ls-files -d`
- `rtk git ls-files --others --exclude-standard`

## Receipts

- `rtk bun run typecheck` - passed.
- `rtk bun turbo test` - passed.
- `rtk bun run lint` - passed with existing advisories and warnings.
- `rtk bun run --cwd packages/web build` - passed.
- `rtk cargo test` in `jnoccio-fusion` - passed, 74 tests.
- `rtk bun run --cwd jnoccio-fusion/web build` - passed.
- `rtk just fast` - passed with advisories.
- `rtk just security` - passed with advisories.
- `rtk just check` - passed and regenerated score artifacts.
- `rtk just score` - passed and regenerated score artifacts.
- `rtk bun run --cwd packages/app test:e2e` - passed as skipped.

## Files Kept

- `packages/ui/src/assets/favicon/apple-touch-icon-v3.png`
- `packages/ui/src/assets/favicon/favicon-96x96-v3.png`
- `packages/ui/src/assets/favicon/favicon-v3.ico`
- `packages/ui/src/assets/favicon/favicon-v3.svg`
  - Keep.
  - Restored the tracked favicon targets required by `packages/app/public` and `packages/web/public` symlinks.

- `packages/app/e2e/todo.spec.ts`
  - Keep.
  - Fix: replaced the invalid `test.fix_item()` call with `test.skip()`.
  - Annotation normalized back to `todo`.

- `agent/repo-score.json`
- `agent/repo-score.md`
- `agent/score-history.jsonl`
- `agent/score-history.csv`
  - Keep.
  - Regenerated by `rtk just check` and `rtk just score`.

## Files Removed

- None in this session.

## Prior Disposition

- Keep
  - `packages/ui/src/assets/favicon/apple-touch-icon-v3.png` deletion kept
  - `packages/ui/src/assets/favicon/favicon-96x96-v3.png` deletion kept
  - `packages/ui/src/assets/favicon/favicon-v3.ico` deletion kept
  - `packages/ui/src/assets/favicon/favicon-v3.svg` deletion kept
  - `packages/ui/src/assets/icons/file-types/folder-backup-open.svg` deletion kept
  - `packages/ui/src/assets/icons/file-types/folder-backup.svg` deletion kept
  - `packages/web/src/assets/lander/copy.svg` deletion kept
  - `packages/console/app/src/asset/lander/copy.svg` deletion kept
  - `packages/console/app/src/asset/brand/jekko-brand-assets.zip` deletion kept
  - `agent/repo-score.json` and `agent/repo-score.md` kept for regeneration only

- Fix First
  - `JANKURAI_TASKLIST.md` protocol drift fixed by normalizing `Status: Completed` to `Status: Complete`

- Restore
  - None. No deleted asset under the scoped build paths had a live import requiring restore.

- Remove
  - `JANKURAI_TASKLIST.md.bak`
  - `JANKURAI_TASKLIST.md.corrupted_backup`

## Evidence

- `packages/web/src/components/Lander.astro` now imports `../assets/lander/clipboard.svg`, not `copy.svg`
- `packages/ui/src/components/favicon.tsx` references non-v3 favicon files that still exist
- `packages/web/astro.config.mjs` references the same non-v3 favicon files
- `packages/console/app/src/component/email-signup.tsx` still uses `default_value`, but that is out of worker 3 scope

## Notes

- The worktree still contains a large set of pre-existing dirty files outside this session's changes.
- No generated SDK output was edited manually.

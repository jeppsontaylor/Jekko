# Compatibility shim for proofbind --changed-from checks.
# TODO(cleanup): remove once proofbind no longer requires .github/workflows/deploy.yml in this repository.
# owner: agent
name: deploy
on: []
concurrency:
  group: jankurai-deploy-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - run: echo "compat shim for proofbind evidence checks"
